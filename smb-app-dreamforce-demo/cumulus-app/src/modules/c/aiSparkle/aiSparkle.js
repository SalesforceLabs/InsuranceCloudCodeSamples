import { LightningElement } from 'lwc';

/**
 * c-ai-sparkle - the bare SLDS 2 Agentforce sparkle glyph.
 *
 * Use this in a card title to mark the card's content as AI-authored,
 * the way the org's Meeting Playbook page marks Client Summary, Meeting
 * Preparation Brief, Meeting Discussion Guide, and Agentforce Actions.
 *
 * c-ai-chip is the labelled variant - sparkle plus text plus a tooltip -
 * and is the right call when the provenance needs explaining. Reach for
 * this one when the title already says what the card is and all you need
 * is the mark. It scales to the font-size of whatever it sits beside.
 */
export default class AiSparkle extends LightningElement {}
